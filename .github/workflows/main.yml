name: Build Maven Project

on:
  push:
    branches:
      - main 
jobs:
  build_and_upload:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build with Maven
        run: mvn clean install

      - name: Run Unit test case
        run: mvn test

      - name: Archive Artifacts
        uses: actions/upload-artifact@v2
        with:
          name: my-artifact  
          path: target/
       
      - name: Jfrog Artifactory
        uses: jfrog/setup-jfrog-cli@v3
        env:
          JF_URL: ${{ secrets.JF_URL }}
          JF_ACCESS_TOKEN: ${{ secrets.JF_ACCESS_TOKEN }}
      
      - name: Artifactory Ping
        env:
          ARTIFACTORY_URL: ${{ secrets.ARTIFACTORY_URL }}
          ARTIFACTORY_API_KEY: ${{ secrets.ARTIFACTORY_API_KEY }}
        run: |
          jf rt ping --url="${ARTIFACTORY_URL}"
      
      # - name: Configure JFrog CLI
      #   env:
      #     ARTIFACTORY_URL: ${{ secrets.ARTIFACTORY_URL }}
      #     ARTIFACTORY_USERNAME: ${{ secrets.ARTIFACTORY_USERNAME }}
      #     ARTIFACTORY_API_KEY: ${{ secrets.ARTIFACTORY_API_KEY }}
      #   run: |
      #         echo "Setting up JFrog CLI"
      #         jfrog rt config --url="${ARTIFACTORY_URL}" --user="admin" --apikey="${ARTIFACTORY_API_KEY}" --interactive=false
      #         jfrog config add  --artifactory-url=<artifactory-url>"
    
  
      # - name: Build with Maven
      #   run: mvn clean install
  
      # - name: Upload Artifacts to Artifactory
      #   run: |
      #       echo "Uploading artifacts to Artifactory"
      #       jfrog rt u "path/to/artifact.ext" "sample-project/"